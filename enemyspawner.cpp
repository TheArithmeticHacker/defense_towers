#include "enemyspawner.h"

EnemySpawner::EnemySpawner(QGraphicsScene * parent)
{
    parentScene = parent;
}

void EnemySpawner::spawnEnemy()
{
    /*
    Enemy* enemy = new Enemy(200, 200);
    activeEnemies.append(enemy);
    parentScene->addItem(enemy);
*/
}
